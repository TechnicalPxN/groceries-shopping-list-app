{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "bun install",
        "buildCommand": "npx expo export -p web"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/_expo/static/html/index.html"
    }
  ]
}